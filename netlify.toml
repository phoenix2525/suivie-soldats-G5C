[build]
  base      = "frontend"
  command   = "npm run build"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "20"

# Règle critique pour React Router : toutes les routes → index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
